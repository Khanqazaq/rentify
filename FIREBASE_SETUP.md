# Firebase Setup Instructions / Инструкция по настройке Firebase

## Шаг 1: Создание проекта Firebase

1. Перейдите на https://console.firebase.google.com/
2. Нажмите "Добавить проект" (Add project)
3. Название проекта: `rentify-kz`
4. Отключите Google Analytics (не обязательно для начала)
5. Нажмите "Создать проект"

## Шаг 2: Настройка веб-приложения

1. В консоли Firebase нажмите на иконку `</>` (Web)
2. Название приложения: `Rentify Web`
3. НЕ включайте Firebase Hosting пока
4. Нажмите "Зарегистрировать приложение"

## Шаг 3: Получение конфигурации

Firebase покажет вам конфигурацию вида:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSy...",
  authDomain: "rentify-kz.firebaseapp.com",
  projectId: "rentify-kz",
  storageBucket: "rentify-kz.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:abcdef1234567890"
};
```

**ВАЖНО:** Скопируйте эту конфигурацию и замените в файле `js/firebase-config.js`

## Шаг 4: Включение Authentication (Авторизация)

1. В левом меню выберите "Authentication" → "Get started"
2. Выберите "Phone" (Телефон)
3. Включите провайдер "Phone"
4. Сохраните

## Шаг 5: Включение Firestore Database (База данных)

1. В левом меню выберите "Firestore Database" → "Create database"
2. Выберите "Start in test mode" (Тестовый режим)
3. Выберите регион: `europe-west1` (Бельгия) или ближайший
4. Нажмите "Enable"

### Настройка правил безопасности:

В разделе "Rules" замените правила на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи могут читать и писать свои данные
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Все могут читать вещи, но создавать только авторизованные
    match /items/{itemId} {
      allow read: if true;
      allow create: if request.auth != null;
      allow update, delete: if request.auth != null && resource.data.ownerId == request.auth.uid;
    }
    
    // Чаты доступны только участникам
    match /chats/{chatId} {
      allow read, write: if request.auth != null;
      
      match /messages/{messageId} {
        allow read, write: if request.auth != null;
      }
    }
  }
}
```

## Шаг 6: Включение Storage (Хранилище файлов)

1. В левом меню выберите "Storage" → "Get started"
2. Оставьте правила по умолчанию
3. Выберите тот же регион что и для Firestore
4. Нажмите "Done"

### Настройка правил Storage:

В разделе "Rules" замените на:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /items/{itemId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    match /users/{userId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

## Шаг 7: Обновление конфигурации в коде

Откройте файл `js/firebase-config.js` и замените конфигурацию на вашу:

```javascript
const firebaseConfig = {
    apiKey: "ВАШ_API_KEY",
    authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
    projectId: "ВАШ_PROJECT_ID",
    storageBucket: "ВАШ_PROJECT_ID.appspot.com",
    messagingSenderId: "ВАШ_SENDER_ID",
    appId: "ВАШ_APP_ID"
};
```

## Готово! 🎉

Теперь ваше приложение подключено к Firebase с:
- ✅ Авторизацией по телефону
- ✅ База данных Firestore (вещи, пользователи, чаты)
- ✅ Хранилище для фотографий
- ✅ Real-time обновления чатов

## Проверка работы

1. Откройте консоль браузера (F12)
2. Должно быть сообщение: "Firebase initialized successfully!"
3. Попробуйте зарегистрироваться
4. Добавьте вещь
5. Проверьте Firebase Console - данные должны появиться в Firestore

## Бесплатный лимит Firebase Spark Plan:

- **Firestore:** 50,000 чтений/день, 20,000 записей/день
- **Storage:** 5 GB хранения, 1 GB загрузки/день
- **Authentication:** Безлимит

Этого достаточно для тестирования и небольшого проекта!

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера на ошибки
2. Убедитесь что правила Firestore и Storage настроены правильно
3. Проверьте что Phone Authentication включен

---

**Важно:** После настройки Firebase все данные будут храниться в облаке, а не в localStorage браузера!
